<h3><%= entry.title %></h3>
<p><%= entry.created_at %> by <%= entry.author %></p>
<p><%= entry.description%> </p>
<%= link_to 'Delete this', entry_path(entry), method: :delete, data: { confirm: 'Really? '} %>
